alias -g pbc="xclip -i -selection clipboard"
alias -g pbp="xclip -o -selection clipboard"

alias la="ls -a"

alias ijs="node"
alias clj="lein repl"

function rinit() {
  git init
  rspec --init
  mkdir lib
  git add -A
  git commit -m "Initial commit"
}
